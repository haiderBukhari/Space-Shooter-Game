#include "game.h"
// # include "./hfiles/Addon.h"
int main()
{
    Game game;
    game.start_game();
    // Addon*initial = new fire(3981, 5591);
    // Addon*initial1 = new Powerup();
    // Addon*initial2 = new Danger(); 
    // game.accept_addon();
    // m.display_menu(); 
    return 0;
}
